<div id="carouselExampleSlidesOnly" class="carousel slide" data-ride="carousel">
  <div class="carousel-inner">
    {{#each content.contentSlots}}
      {{#if (eq @index 0)}}
        <div class="carousel-item active">
      {{else}}
        <div class="carousel-item">
      {{/if}}
      {{#if (eq [@type] 'http://c1-qa.adis.ws/c/cmslabs/image-block')}}
          <img class="d-block" src="http://{{image.defaultHost}}/i/{{image.endpoint}}/{{image.name}}?w=600&h=250" border=0 />
      {{/if}}
      {{#if (eq [@type] 'http://c1-qa.adis.ws/c/cmslabs/content-stack')}}
        {{> slides }}
      {{/if}}
      </div>
    {{/each}}
  </div>
  <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>
